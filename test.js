 {/* <TouchableOpacity
        onPress={async () => {
          let devices =
            await Voximplant.Hardware.AudioDeviceManager.getInstance().getAudioDevices();
          Voximplant.Hardware.AudioDeviceManager.getInstance().selectAudioDevice(
            devices[1],
          );
        }}>
        <Text style={styles.contactName}>{433}</Text>
      </TouchableOpacity> */}